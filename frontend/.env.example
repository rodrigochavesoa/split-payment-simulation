# Desenvolvimento: o Vite encaminha /api para esta origem, sem exigir CORS no Spring.
VITE_BACKEND_ORIGIN=http://localhost:8080

# Produção: deixe vazio quando o dashboard e a API estiverem sob a mesma origem.
# VITE_API_BASE_URL=https://api.exemplo.com
