# HTTP port exposed by docker compose.
APP_PORT=8080

# Spring profile selected for local container execution.
SPRING_PROFILES_ACTIVE=local

# JVM options. Do not place credentials or secrets in this file.
JAVA_TOOL_OPTIONS=-XX:MaxRAMPercentage=75.0 -Djava.security.egd=file:/dev/./urandom
